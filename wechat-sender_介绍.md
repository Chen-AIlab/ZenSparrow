# 微信发送 Skill

WorkBuddy 通过模拟键盘操作，自动帮你给微信联系人或群聊发消息。

## 能做什么

- 给指定联系人/群聊发送文本消息
- 发送 Word、图片等文件（文件需在本地磁盘上）
- 自动追加签名 `—— workbuddy使用mac发送`，区分人工和自动发送
- 每次发送后自动记录日志，可回溯

## 不能做什么

- 不支持发送语音、视频通话、红包等
- 不能读取微信聊天记录（只看不发）
- 不能主动添加好友、建群
- 群名/联系人名必须与微信中显示的名称完全一致才能搜到

## 怎么用

在 WorkBuddy 对话里直接说人话就可以，比如：

```
用微信给张三发一条：会议推迟到3点
```

```
给运营日报群发一条：今日数据已更新
```

```
发一个文件给张三，文件在桌面上叫 周报.docx
```

Skill 会自动激活微信、搜索联系人、粘贴消息、回车发送，全程无需你动手。

## 前提

1. Mac 上的微信客户端必须登录且在运行
2. 系统设置 → 隐私 → 辅助功能 → 已授权的 App 列表里要有 WorkBuddy/Terminal

## 小技巧

如果发消息时说"找到XX群"，有时候 WorkBuddy 会误以为是联网搜索。没关系，脚本会自动把中文群名转成拼音去搜（比如"组长群"→"zuzhangqun"），一样能匹配到。
